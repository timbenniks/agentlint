You are evaluating whether developer documentation is sufficient for an agent to complete an integration.

Use ONLY the supplied evidence.
Do not use external knowledge.

Determine whether the docs answer:

- What is this?
- When should I use it?
- How do I get started?
- What prerequisites exist?
- How do I authenticate?
- What is the smallest working example?
- How do failures behave?
- Where is the API/schema reference?

This result supplements deterministic OpenAPI checks; do not re-judge specification parseability.

Return structured JSON only.
